/*
  TODO(bootstrap): add minimal seed data in implementation phase
  after schema models are introduced.
*/

async function main(): Promise<void> {
  console.log("Bootstrap seed placeholder. No domain data is seeded.");
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
